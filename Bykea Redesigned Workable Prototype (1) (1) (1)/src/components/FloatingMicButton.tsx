import { Mic } from "lucide-react";

interface FloatingMicButtonProps {
  onClick: () => void;
}

export function FloatingMicButton({ onClick }: FloatingMicButtonProps) {
  return null; // Temporarily disabled - floating outside frame
}